ATARI_PROJECTS=$(HOME)/Projects/atari-projects

# Path to cc65 platform files (headers + libraries).
# Default works if cc65 was built from source with: make install PREFIX=~/.local
# CC65_HOME=$(HOME)/.local/share/cc65
